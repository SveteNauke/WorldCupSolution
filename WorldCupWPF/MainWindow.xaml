<Window x:Class="WorldCupWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WorldCupWPF"
        mc:Ignorable="d"
        Title="MainWindow" Height="500" Width="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Button Content="⚙" Click="BtnReopenSettings_Click" Width="30" Height="30"
                HorizontalAlignment="Right" VerticalAlignment="Top" Margin="0,0,0,10" />
        
        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Omiljena reprezentacija:" VerticalAlignment="Center" Margin="0,0,10,0" />
            <ComboBox x:Name="cmbFavoriteTeam" Width="200" SelectionChanged="cmbFavoriteTeam_SelectionChanged" />
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0,0,0,10">
            <TextBlock Text="Protivnik:" VerticalAlignment="Center" Margin="0,0,10,0" />
            <ComboBox x:Name="cmbOpponent" Width="200" Height="30" SelectionChanged="cmbOpponent_SelectionChanged" />
        </StackPanel>

        <TextBlock x:Name="txtResult" Grid.Row="2" FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,10" />

        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,20">
            <Button Content="Info o omiljenoj" Click="BtnFavoriteInfo_Click" Width="250" Margin="10" />
            <Button Content="Info o protivniku" Click="BtnOpponentInfo_Click" Width="250" Margin="10" />

            
            
        </StackPanel>
    </Grid>
</Window>
